name: Deploy to UAT

on:
  push:
    branches:
      - uat

jobs:
  deploy:
    runs-on: windows-latest  # or 'ubuntu-latest' based on your needs

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy SQL script
        run: |
          echo "Pretend we’re deploying here..."
          # Add sqlcmd or PowerShell commands here
